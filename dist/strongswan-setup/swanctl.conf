    connections {
        net-net {
            remote_addrs = REMOTE_IPSEC_VPN_GW_IP

            local {
                auth = pubkey
                certs = LOCAL_CERT_PEM
            }
            remote {
                auth = pubkey
                id = "C=CH, O=strongSwan, CN=sun.strongswan.org"
            }
            children {
                net-net {
                    local_ts  = LOCAL_SUBNET_CIDR
                    remote_ts = REMOTE_SUBNET_CIDR
                    start_action = trap
                }
            }
        }
    }
